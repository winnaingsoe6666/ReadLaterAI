---
name: Database Management Skill
description: Trigger this skill when working with the local SQLite database. Contains instructions for database setup, schema generation (content, tags, summaries), SQL migrations, transaction handling, and query optimization.
---

# Database Management

## Purpose
- Setup SQLite database locally
- Schema generation for `content`, `tags`, `content_tags`, `summaries`
- Handle database migrations and queries

## Instructions
- Ensure you manage the local SQLite instance.
- Run migrations when the schema updates.
