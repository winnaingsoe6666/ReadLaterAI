server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: jdbc:sqlite:${KNOWVAULT_DATA_DIR:./data}/knowvault.db
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

logging:
  level:
    com.knowvault: INFO
    org.springframework: WARN
